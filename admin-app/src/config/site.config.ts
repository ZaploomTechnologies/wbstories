// Admin-only branding — no public nav/url/ogImage fields needed here since
// this app has no public-facing pages.
export const siteConfig = {
  name: "WB Stories Admin",
  builtBy: {
    name: "Zaploom Technologies",
    url: "https://zaploom.in/",
  },
} as const;
